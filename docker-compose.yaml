services:
  quicknotes:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - quicknotes-data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  quicknotes-data:
